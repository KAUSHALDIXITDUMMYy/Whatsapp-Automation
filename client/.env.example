# Public API URL (no trailing slash).
# Leave empty when using Vercel — client/vercel.json proxies /api to the backend.
# Set explicitly if the UI is hosted elsewhere without that proxy.
# Example: https://38-248-12-151.sslip.io
VITE_API_URL=
